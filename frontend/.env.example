# Copy to .env for local overrides, or set in the Vercel dashboard.
# URL of the deployed backend (Render). Leave empty for local dev, where Vite
# proxies /api and /ws to localhost:4000.
VITE_API_URL=https://trade-desk-api.onrender.com
